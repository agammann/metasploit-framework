# Custom modules

Place your own modules here using Metasploit's normal category layout, for
example `auxiliary/scanner/<name>.rb`. The start script mounts this directory
at the Framework user module path. In a running console, use `reload_all`
after adding or changing a module.
